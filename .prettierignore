trash
apps/mobile/android
apps/mobile/ios

# Cloudflare Workers Static Assets config — not Markdown. Prettier's markdown
# parser escapes `*`/`_` and reflows these, corrupting path matching.
**/_headers
**/_redirects

# Deterministic generated source of truth; byte equality is checked by its materializer.
apps/stella/content/guardian-cards/production-art-batches-ko.json
