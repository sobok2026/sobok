/**
 * The floating pill every header is built from. Shared so the paid funnel's stripped-down header reads as
 * the same site as the marketing one — it drops the nav and the locale switcher, not the visual language.
 */
export const HEADER_ISLAND =
  'h-9 rounded-full border border-border bg-background/70 px-4 shadow-lg shadow-black/20 backdrop-blur-md'
