name: Backend Integration

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: backend-integration-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-integration:
    name: Backend integration
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CI: true
      BACKEND_INTEGRATION_POSTGRES_URL: postgresql://test_user:test_password@127.0.0.1:5432/sobok_backend_integration_test
      UPSTASH_KV_REST_API_TOKEN: local_dev_token
      UPSTASH_KV_REST_API_URL: http://127.0.0.1:8079
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: postgres
        options: >-
          --health-cmd "pg_isready -U test_user -d postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432

      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping || exit 1"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20

      srh:
        image: hiett/serverless-redis-http:latest
        env:
          SRH_MODE: env
          SRH_TOKEN: local_dev_token
          SRH_CONNECTION_STRING: redis://redis:6379
        ports:
          - 8079:80

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.12

      - name: Restore Bun cache
        uses: actions/cache@v5
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock', 'package.json') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Wait for Redis HTTP bridge
        shell: bash
        run: |
          set -euo pipefail

          for attempt in $(seq 1 30); do
            if curl --fail --silent --show-error \
              -X POST "${UPSTASH_KV_REST_API_URL}" \
              -H "Authorization: Bearer ${UPSTASH_KV_REST_API_TOKEN}" \
              -H "Content-Type: application/json" \
              -d '["PING"]' >/dev/null; then
              exit 0
            fi

            sleep 1
          done

          echo "Redis HTTP bridge did not become ready in time" >&2
          exit 1

      - name: Run backend integration tests
        run: bun run test:backend:integration
